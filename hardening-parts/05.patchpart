-  const encodedFullName = requestHeaders.get(USER_FULL_NAME_HEADER);
-  const fullName =
-    encodedFullName &&
-    requestHeaders.get(USER_FULL_NAME_ENCODING_HEADER) === PERCENT_ENCODED_UTF8
-      ? safeDecodeURIComponent(encodedFullName)
-      : null;
-
-  return {
-    displayName: fullName ?? email,
-    email,
-    fullName,
-  };
-}
-
-export async function requireChatGPTUser(
-  returnTo: string,
-): Promise<ChatGPTUser> {
-  const user = await getChatGPTUser();
-  if (user) return user;
-
-  redirect(chatGPTSignInPath(returnTo));
-}
-
-export function chatGPTSignInPath(returnTo: string): string {
-  const safeReturnTo = safeRelativeReturnPath(returnTo);
-  return `${SIGN_IN_PATH}?return_to=${encodeURIComponent(safeReturnTo)}`;
-}
-
-export function chatGPTSignOutPath(returnTo = "/"): string {
-  const safeReturnTo = safeRelativeReturnPath(returnTo);
-  return `${SIGN_OUT_PATH}?return_to=${encodeURIComponent(safeReturnTo)}`;
-}
-
-function safeRelativeReturnPath(value: string): string {
-  if (!value.startsWith("/") || value.startsWith("//")) return "/";
-
-  let url: URL;
-  try {
-    url = new URL(value, "https://app.local");
-  } catch {
-    return "/";
-  }
-  if (url.origin !== "https://app.local") return "/";
-  if (isReservedAuthPath(url.pathname)) return "/";
-
-  return `${url.pathname}${url.search}${url.hash}`;
-}
-
-function isReservedAuthPath(pathname: string): boolean {
-  return (
-    pathname === SIGN_IN_PATH ||
-    pathname === SIGN_OUT_PATH ||
-    pathname === CALLBACK_PATH
-  );
-}
-
-function safeDecodeURIComponent(value: string): string | null {
-  try {
-    return decodeURIComponent(value);
-  } catch {
-    return null;
-  }
-}
diff --git a/app/docsMetadata.ts b/app/docsMetadata.ts
index 1142553..04cea4b 100644
--- a/app/docsMetadata.ts
+++ b/app/docsMetadata.ts
@@ -1,19 +1,21 @@
 import type { Metadata } from "next";
 
 export function docsMetadata(title: string, description: string, canonical: string): Metadata {
+  const fullTitle = `${title} | Accounting Agents`;
   return {
-    title: `${title} | Accounting Agents`,
+    title: fullTitle,
     description,
     alternates: { canonical },
     openGraph: {
-      title: `${title} | Accounting Agents`,
+      title: fullTitle,
       description,
-      images: [],
+      images: [{ url: "/og.png", width: 1200, height: 630, alt: "Accounting Agents field guide" }],
     },
     twitter: {
-      title: `${title} | Accounting Agents`,
+      card: "summary_large_image",
+      title: fullTitle,
       description,
-      images: [],
+      images: ["/og.png"],
     },
   };
 }
diff --git a/app/domain-api.ts b/app/domain-api.ts
index 1d18706..8a71e88 100644
--- a/app/domain-api.ts
+++ b/app/domain-api.ts
@@ -6,7 +6,7 @@ import {
   publicResponse,
   siteOrigin,
 } from "./agent-interface";
-import { corpusReviewedAt, corpusVersion } from "./domain-model";
+import { corpusModifiedAt, corpusReviewedAt, corpusVersion } from "./domain-model";
 import { domainRightsNotice, normalizeDomainRecord } from "./domain-interface";
 
 type IdentifiedRecord = { id: string };
@@ -85,6 +85,7 @@ export async function domainCollectionResponse<T extends IdentifiedRecord>({
         "Content-Language": "en",
         ...(nextUrl ? { "X-Next-Page": nextUrl.toString() } : {}),
       },
+      modifiedAt: corpusModifiedAt,
     });
   }
 
@@ -113,6 +114,7 @@ export async function domainCollectionResponse<T extends IdentifiedRecord>({
 
   return publicResponse(request, body, "application/json; charset=utf-8", {
     headers: nextUrl ? { "X-Next-Page": nextUrl.toString() } : undefined,
+    modifiedAt: corpusModifiedAt,
   });
 }
 
@@ -144,6 +146,7 @@ export function domainItemResponse<T extends IdentifiedRecord>({
   if (format === "markdown") {
     return publicResponse(request, renderMarkdown([item]), "text/markdown; charset=utf-8", {
       headers: { "Content-Language": "en" },
+      modifiedAt: corpusModifiedAt,
     });
   }
 
@@ -156,5 +159,6 @@ export function domainItemResponse<T extends IdentifiedRecord>({
       item: normalizeDomainRecord(item, collection),
     }, null, 2),
     "application/json; charset=utf-8",
+    { modifiedAt: corpusModifiedAt },
   );
 }
diff --git a/app/domain-model.ts b/app/domain-model.ts
index bf6416b..8950a78 100644
--- a/app/domain-model.ts
+++ b/app/domain-model.ts
@@ -1,7 +1,15 @@
-export const corpusReviewedAt = "2026-08-23";
-export const corpusModifiedAt = "2026-08-23T00:00:00.000Z";
-export const corpusVersion = "2026-08-23.5";
-export const domainSchemaVersion = "1.0";
+export {
+  corpusModifiedAt,
+  corpusReviewedAt,
+  corpusVersion,
+  domainSchemaVersion,
+} from "./project-metadata";
+import {
+  corpusModifiedAt,
+  corpusReviewedAt,
+  corpusVersion,
+  domainSchemaVersion,
+} from "./project-metadata";
 
 export type AuthorityLevelId = "A0" | "A1" | "A2" | "A3" | "A4" | "human-only";
 
diff --git a/app/downloads/reading-room.json/route.ts b/app/downloads/reading-room.json/route.ts
index 0b43885..9fdcf4b 100644
--- a/app/downloads/reading-room.json/route.ts
+++ b/app/downloads/reading-room.json/route.ts
@@ -39,7 +39,9 @@ export async function GET(request: Request) {
     })),
   }, null, 2);
 
-  return publicResponse(request, body, "application/json; charset=utf-8");
+  return publicResponse(request, body, "application/json; charset=utf-8", {
+    modifiedAt: catalogModifiedAt,
+  });
 }
 
 export const HEAD = GET;
diff --git a/app/downloads/resources.json/route.ts b/app/downloads/resources.json/route.ts
index 81a2c36..4cffced 100644
--- a/app/downloads/resources.json/route.ts
+++ b/app/downloads/resources.json/route.ts
@@ -1,6 +1,7 @@
 import {
   agentResources,
   apiVersion,
+  catalogModifiedAt,
   catalogReviewedAt,
   catalogVersion,
   publicResponse,
@@ -18,6 +19,7 @@ export async function GET(request: Request) {
   }, null, 2);
 
   return publicResponse(request, body, "application/json; charset=utf-8", {
+    modifiedAt: catalogModifiedAt,
     headers: {
       "Content-Disposition": "inline; filename=accounting-agents-resources.json",
       "Content-Language": "en",
diff --git a/app/layout.tsx b/app/layout.tsx
index 286d1a1..c7ac057 100644
--- a/app/layout.tsx
+++ b/app/layout.tsx
@@ -1,8 +1,9 @@
 import type { Metadata } from "next";
 import "./globals.css";
+import { siteOrigin } from "./project-metadata";
 
 export const metadata: Metadata = {
-  metadataBase: new URL("https://accounting-agents.madebyhenry.chatgpt.site"),
+  metadataBase: new URL(siteOrigin),
   title: "Accounting Agents",
   description:
     "Methods, workflow examples, control guidance, and primary sources for accounting teams using AI agents.",
@@ -54,7 +55,9 @@ export default function RootLayout({
   return (
     <html lang="en">
       <head>
-        <meta content="development" name="codex-preview" />
+        {process.env.CODEX_PREVIEW === "development" && (
+          <meta content="development" name="codex-preview" />
+        )}
         <link href="/agent-context.md" rel="alternate" type="text/markdown" />
         <link href="/llms.txt" rel="describedby" type="text/plain" />
         <link href="/.well-known/api-catalog" rel="api-catalog" type="application/linkset+json" />
diff --git a/app/machine-access/page.tsx b/app/machine-access/page.tsx
index bd295f9..1600813 100644
--- a/app/machine-access/page.tsx
+++ b/app/machine-access/page.tsx
@@ -156,7 +156,7 @@ export default function MachineAccessPage() {
