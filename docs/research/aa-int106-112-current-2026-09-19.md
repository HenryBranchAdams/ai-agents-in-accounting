# Finance and healthcare current integration

Edition 2026-09-19.12424 integrates the independently reviewed finance and healthcare source packages after support and waste. The selected roles remain separate and all ten new assessments remain partial. Earlier record objects, primary source metadata, rights, mappings and immutable history remain preserved.

The finance amendment synchronizes corrected question locators into the guide, makes earned-premium and supplied-claim assumptions explicit, and leaves an unexplained statutory difference unposted. SSAP 62 supports the selected reinsurance and deposit route only. Custody totals do not compute reserve requirements or establish ownership. The original author commits remain preserved. Healthcare retains its independently accepted source packet unchanged, including distinct provider worksheets, claims and PLB reconciliation, charity-cost disclosure and separate reporting rules.

Both current importers stage conflicts before writing, preserve prior objects and support unchanged replay. The source fixtures use pinned baseline data, real corpus validation and actual agent retrieval. Current retrieval, full checks, immutable release and archive verification, independent integrated-head acceptance and CI are separate completion evidence. No deployment is included.

The expanded frozen history exceeded the default 4 GiB TypeScript heap during the first full check. A measured comparison using the same compiler, workload and 6 GiB ceiling reduced elapsed time from 55.36 seconds to 15.13 seconds and reported compiler memory from 4,286,463 KiB to 950,106 KiB. A small JavaScript re-export with an unknown TypeScript declaration stops static inference across the immutable historical JSON. Current corpus and assessment JSON remain in type checking, as a compiler-input regression verifies. Existing checksum, schema, archive and runtime/download history-equality checks retain the data verification boundary; no historical data is removed or rewritten. This is a local measurement, not a CI or runtime-latency claim.

The first measured edition, 2026-09-19.12423, predates the typecheck boundary. Its snapshot and six frozen release files remain byte-identical. Edition 2026-09-19.12424 records the final code and coverage inputs with the same research records. The local HTTP check also requires ordinary loopback socket access; a sandbox-denied attempt is not an application failure or a passing check.
