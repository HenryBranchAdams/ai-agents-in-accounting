# Issue 173 validation and acceptance

This is a working evidence register, not a completion certificate. All nine packages remain subject to the integration, source-scope and maintainer conditions in issue173. PR181 is included in the combined source baseline. No research release has been published.

## Inputs and current integration

Main baseline: `186b6b14cc43f0d68c650e877d77719a075d4a72`, including PR181. Research content at `041eaef49b1147ffa521816741352797fd69da40` has1,532 records:53 added,18 changed and none removed relative to that main. It includes906 sources,276 guides and38 collections. Historical release and coverage snapshot paths are byte-unchanged from main in the editing checkout.

The separate isolated candidate `cc1a5109563968ae3160bff37a959d1efbc7e67817b845430f0a7ed1c14e1b23` prepares prospective edition `2026-09-22.1`. It is not finalized or publishable. Preparation initially failed because the local npm cache lacked PR181's pinned Playwright dependency. `npm ci` installed the lockfile successfully; the retained retry prepared the candidate. The failed attempt remains available outside the export inventory.

A direct draft preview first stopped at stale coverage mappings. The supported isolated preparation path regenerated the candidate's mappings and prospective history without promoting them to canonical source. This preparation cost is additional work, not eliminated preview work.

The prepared preview identity is `571ea80ae99678adaef8b5156db9114d62313a592344acb476357eede8694c02`, input digest `b7fe4630eb3476f509d9c20b2af6644606ce2014081b4e24db1029a7881e2142`. It built in3.19seconds with source revision041eaef and Node22.23.1. This measures the preview build alone, not dependency installation, candidate preparation, full verification, artifact transfer or release publication.

## Observed checks

| Check | Result | Limit |
| --- | --- | --- |
| Record/schema/dependency validation in editing checkout | Reaches expected stale draft coverage boundary | Overall validation is not green until preparation/finalization supplies matching generated inputs |
| Connected accounting cases and approval fixtures |11 tests pass at f14fc76 | Original arithmetic/replay/payload checks, not observed API behavior or approval |
| Source-review import safeguards |2 tests pass for empirical reconciliation | Does not independently verify publisher claims |
| New research-depth checks against prepared preview |6 tests pass | Compiled preview, not production artifact or browser |
| Historical source paths compared with main | No changes under releases or coverage snapshots | Final finalized edition must repeat preservation check |

The six preview checks exercise eleven search queries spanning all packages through both full-record and compact retrieval, rendered HTML/Markdown qualifications and source links, scope counterexamples, five qualified edges retaining seven owned annotations, case posture/asset permission exclusions, and changed-source freshness invalidation. The test file was evaluated separately against the frozen preview; its hash and preview inputs are recorded in the operational receipt. It will also run through the normal production-built suite.

`tests/research-depth-browser.test.mjs` adds the reusable production-built desktop/mobile, JavaScript/no-JavaScript reading journey, keyboard source navigation, Back navigation, overflow, same-origin request and console checks. It has not yet run. Local browser startup and loopback are unavailable in this session; hosted Playwright remains the supported fallback. PR181's existing browser journeys remain part of the required suite.

## Package evidence and remaining acceptance

| Package | Canonical entry and material | Remaining condition |
| --- | --- | --- |
| P1 | Xero/QBO ledger-completeness guides, official endpoint/recovery/attachment profiles, original gap fixtures, narrow Oracle/NetSuite comparison | Finish the bounded unresolved-contract register; test final retrieval/rendering and preserve unobserved runtime limits |
| P2 | Both connected cases, document/event manifests, journals, reconciliations, alternatives and three extensions | Final production export/render consistency and reader acceptance |
| P3 | Independent-deployment guide, four scoped methods profiles, six-additional-record screen | Preserve excluded full-text/private-input/preregistration dependencies; final presentation and acceptance |
| P4 | Evaluation-selection guide, FinBalance/APEX/FinQA/CORD profiles and local structure checks | Preserve private components, CORD methods/grader and upstream rights limits; final integration |
| P5 | Six-case failure collection across distinct mechanisms | Final browser/export/source-locator review and acceptance |
| P6 | Action-boundary guide, five revised controls, synthetic approval and recovery cases | Renew affected control editorial records; no operating-effectiveness claim |
| P7 | Stronger and inadequate case packets and unexecuted human-measurement protocol | Human usability and maintainer decisions remain separate from fixture tests |
| P8 | Seven counterexamples with five explicit qualified edges | Final171 graph/List direction, owner/locator and interaction checks |
| P9 | Eight-candidate assets collection with purpose-specific dispositions | Retain three unresolved exclusions and exact sample-check limits; final exports and acceptance |

## Outstanding release gates

Complete source-scope reconciliation and affected editorial reviews; preserve relevant input digests; finalize through the accepted edition path; run the complete repository check and hosted production browser suite; obtain the specified human acceptance; review and merge the compatible implementations. The final integrated171/175 interface must be tested against this corpus and PR181 content. Then consume the authenticated successful main-CI artifact, perform staged import and native Sites publication, remove the temporary credential, and verify the final live deployment. No earlier preview, receipt, existing approval or main artifact substitutes for those gates.

Operational logs, downloaded source material, screenshots and receipts remain in temporary/ignored output locations excluded from source/export inventory. This committed validation document and the reusable test code intentionally are source inputs.

The source input inventory checked after adding these tests contains947 files and no `outputs/`, `.sites-runtime/`, `node_modules/` or `dist/` paths. `npm run check` refused before build/tests while the new source files were uncommitted and because loopback binding returnedEPERM. This refusal is not a test pass, and the listener check was not bypassed.

## Subsequent source and control reconciliation

The five control extensions received a scoped agent editorial review, preserved in `data/reviews/issue-173-control-editorial.json`; prior reviews remain in their canonical records. The action-boundary and failure-case briefs were rechecked against the changed controls before their bindings were renewed. This does not supply human or professional acceptance.

Xero's scope-resource map and additive consent were read from the original public documentation, and the bank-statement boundary was rechecked. The resulting source profile and both dossiers' bounded dependency registers identify remaining configuration, inaccessible contract and intended-use evidence explicitly. The earlier1532-record preview is now superseded for affected content; its receipt remains evidence of that earlier input only. Eleven connected-case and approval-fixture checks passed after these changes. Final source counts, preview/build inputs, browser results and full CI must be renewed.

## Production candidate check before finalization

Candidate `8da67abe1f90ae723281a7fcb8b55c81056ee043475dc3425f1b31869e4dc6a6` captures source `7dcb765` and prepared input digest `78f8f55d66270a755e65485c7775621702c3c46af9cf3e4b39da740f16138e21`. Lint passed, including typecheck, prepared-data validation and all11 design violation probes. The production build took84.34seconds and produced1,533 records,35 downloads and a954-file multipart source export. Build digest: `ab0cf3883cd8167cea745ec5edcd696e9274cd92bf39dfd3e76d99e06fccc946`.

Ten focused test files covered research reading/retrieval, connected cases, approvals, knowledge relationships, PR181 family-office integration, historical snapshots and clean-directory source reconstruction. The first run passed56/57. The failure was the issue170 preservation test's requirement that current historical-cohort records never receive later research updates. It was narrowed to an explicit19-record amendment register, while retaining unchanged rights, original URLs, identity and migration provenance, exact prior empirical/control reviews, and the full previous invariant for every other record. The corrected six-test editorial file passes against the same production output. This is a focused regression recheck, not a claimed fresh full57-test run or full repository check.

The failed candidate and logs remain outside source inventory. Its edition has not been finalized. Test/document changes require a new prepared candidate before promotion. Hosted full CI, both browser suites, human acceptance and publication remain outstanding.

## Research edition finalization

Candidate `0a028872c0034eea2b75eff27673ccb348618aab533ecaf7808d7aa2035dd476`, based on420ea58, was reviewed and finalized through `npm run edition -- finalize`. It superseded the earlier draft candidates without modifying their retained evidence. All453 existing historical release/snapshot files were compared byte-for-byte before promotion and preserved. The17 generated changes contain only current metadata/indexes and the new2026-09-22.1 release/snapshot. No historical object was overwritten.

`npm run validate` passes for1,533 records and907 sources at2026-09-22.1. The isolated dossier/source-review tests also pass4/4. This is a finalized corpus edition for PR verification, not a verified main artifact, human acceptance or publication. Subsequent content corrections must follow the established supersession policy.
