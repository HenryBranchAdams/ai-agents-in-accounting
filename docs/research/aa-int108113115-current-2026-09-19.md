# Current integration note: professional, arts and recreation, and other services

Edition `2026-09-19.12426` combines three bounded source packages for the current corpus. The integration adds 19 records, including 5 source records, 18 named research questions, 18 partial assessments, 16 mapping proposals, and 8 curated global retrieval rows. The source packages remain separate evidence packets with their recorded jurisdictions, source periods, rights status, source currency, professional-review status, and operational limits.

The application is additive and field-aware. Existing stable records, questions, assessments, mappings, retrieval rows, coverage history, snapshots, and releases are preserved. A differing existing record, mapping, retrieval row, or corpus edition is rejected before any file is written. Reapplying the same current package is byte-idempotent.

The focused integration checks passed against the public `2026-09-19.12425` base. They covered preservation and replay, pre-write conflict refusal, packet status and retrieval limits, schema and coverage validation, and the bundled agent's actual search, get, and context routes. The retrieval registry uses curated search queries with explicit expected and excluded records, scope assertions, and citation requirements. Get and context fixtures remain dedicated real-agent checks rather than being converted into search queries.

The applied corpus validated with 1,318 records and 761 sources. The generated 12426 snapshot and release artifacts were built only after coverage mappings and coverage inputs were frozen. This work records current integration behavior; it does not establish whole-industry sufficiency, current consolidated authority beyond each source's recorded scope, professional sign-off, operating evidence, empirical agent performance, or publication/deployment status.
