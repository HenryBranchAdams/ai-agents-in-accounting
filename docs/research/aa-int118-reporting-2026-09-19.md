# Reporting foundations current integration

Edition2026-09-19.12416 adds four bounded US answers for ledger close, estimates, financial statement presentation, and policy changes/errors. The source inventory preserves all eight original families and the previously accepted routes for framework selection, consolidation, foreign currency, subsequent events and going concern. This is completion of the selected research routes, not sufficient coverage of every family or current US accounting requirement.

The original three proposal JSON files fromcd2cf8e remain byte-identical. Five proposed source IDs were never canonical and duplicate existing publisher URLs. The integration maps them to `src_1os761s`, `src_fasb_202010`, `src_secsab0099`, `src_secsab0108` and `src_regsxcfr`. Each receives a separately scoped supplemental review. Primary source fields, dates, rights, all prior reviews and all prior mapping overrides remain unchanged. Only the SEC Financial Reporting Manual Topic1 source is added. The ID mapping is an integration rule, not a reassignment or deletion of an existing canonical record.

The new original synthetic example connects period cutoff, changed estimate inputs, statement mapping and policy/error classification. Every answer has source locators, inputs, controls, exceptions and unresolved gaps. SEC staff material remains limited to the stated filer context; FASB ASU2020-10 remains a historical amendment, the Codification landing page supplies access context, and blocked eCFR paragraph review does not become substantive current-authority evidence. The four added assessments remain partial. Current consolidated text, entity facts, professional review and operational or empirical support remain open as recorded.

`integrate-reporting-completion.mjs --current` accepts the assets edition12413, corrected draft12415, or final reporting edition12416. It preflights and stages the additions, edition metadata and four retrieval fixtures, rejects conflicting prior reviews or late retrieval changes, and is byte-stable on replay. Default mode retains source-proposal application with corrected canonical source reuse. Dry-run changes no files. Existing international and accepted US questions keep their original identifiers and payloads.

Current tests exercise all five reused records, prior mapping/question preservation, unknown editions and late conflicts. Actual search/context retrieval checks the four selected guides and their citable evidence, while guide-only search excludes the SEC source as a record-kind counterexample. Canonical assessments remain shared context and grant no new industry sufficiency. Standard generators produce matching mappings, snapshot, release downloads and source archive; independent acceptance and CI are separate from local checks. No deployment is performed.

The retained draft12414 release exposed a missing current-edition review note and three overwritten prior guide package-marker fields. Final12415 restores those original marker fields and stores the new review in a separate reporting_completion marker. Historical replay fixtures pin accepted source objects so they reach their intended later conflict checks; the legacy applicator still rejects newer supplemental source reviews before writing. The current integration has independent preservation and conflict tests. Draft12414 snapshot and release bytes remain immutable.

Final12416 also preserves draft12415: its corpus was correct, but its change files compared against accepted12413 while the build requires the latest retained predecessor12414. The final release compares against retained12415 using the same predecessor as the standard build. Both draft releases remain unchanged.
