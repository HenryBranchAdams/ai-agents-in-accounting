# AA-R136 targeted re-review

Re-review date: 2026-09-17

## Review identity

- Pull request: [#136](https://github.com/HenryBranchAdams/ai-agents-in-accounting/pull/136)
- Integration parent: `0e3aef898d9ebc97978f82659ddf2600d83c1d8d`
- Reviewed author head: `fd9e2b68f50d72998160b618454ffa3dc97d4de7`
- Author branch: `codex/aa-int119125`
- Review branch: `codex/aa-r136-rereview`
- Worktree: `/Users/henryadams/.codex/worktrees/74e8/ai-agents-in-accounting`
- Prior review receipt: `96e7afc`
- Live PR check observed once at the exact head: `verify` was `IN_PROGRESS` at [GitHub Actions run 35269630994](https://github.com/HenryBranchAdams/ai-agents-in-accounting/actions/runs/35269630994/job/105365387384). No polling was performed.

## Outcome

The P1 data-loss finding from the prior review is resolved. The corrected canonical source and candidate release are coherent, and the implementation passes the full exact-head check. One P2 test-evidence defect remains: the new regression does not exercise the corrected new-source branch. The review therefore records runtime integration as passing but does not give a clean integration-acceptance pass until the regression is strengthened.

The review made no canonical-data or author-branch corrections. The only intended change on this branch is this receipt.

## Prior P1 resolution

The shared `supplementalReview` builder at `scripts/integrate-management-accounting.mjs:126-149` is now used by both the existing-source path at `:194-197` and the new-source path at `:282-286`. The canonical `src_far_31203_indirect_costs` record contains the accepted AA-I125 supplemental review with the Acquisition.gov URL, FAR 31.203(b)-(g) locator, FAC 2026-01 effective date, material-read check, limitations and unresolved rights.

All ten accepted component source records match their accepted heads exactly: seven from PR133 and three from PR135. The records retain `source_status: unknown`, `full_text_stored: false`, source review metadata and unresolved source rights. The existing-source path also preserves an unrelated prior supplemental batch while retaining exactly one AA-I125 entry in an isolated replay.

## Finding

### [P2] The clean-integration regression does not exercise the corrected new-source path

The helper in `tests/aa-int119125-integration.test.mjs:9-25` copies the current candidate `data/corpus/source.json` into its temporary harness. At the reviewed head, `src_far_31203_indirect_costs` is already present, while the packet still marks it `new: true`. The importer therefore takes the existing-source branch and calls `applySource`, not `newSource`.

As a result, the test at `tests/aa-int119125-integration.test.mjs:72-99` would still pass if the new-source assignment at `scripts/integrate-management-accounting.mjs:286` were removed. It checks the complete supplemental-review object produced by the existing-source path, but not the complete accepted source record produced when the source is absent. It also checks byte stability only for `source.json`, not every file the harness copies.

Independent evidence shows the implementation itself is correct: a temporary harness built from base `fee5cdbc81b2dfacbc43ff8b9356ee853ca11188`, with the candidate packet and importer, started with the FAR source absent; the first application produced a FAR record byte-identical to accepted PR135, and the second application changed none of the ten integration files.

Required disposition: make the committed regression start from the exact base inputs or remove `src_far_31203_indirect_costs` before the first application, compare the resulting complete FAR record with accepted PR135, and assert that all copied integration files are byte-stable on the second run. This is a test-evidence correction; no runtime data correction is indicated at this head.

## Acceptance matrix

| Criterion | Result | Evidence and limit |
| --- | --- | --- |
| Resolve the prior accepted-package data loss | Pass | Shared builder now populates existing and new sources; the FAR source matches accepted PR135 exactly. |
| Regression exercises clean new-source application and replay | Partial | The implementation passes an independent source-absent base harness, but the committed regression starts with the source already present and does not protect the corrected branch. |
| Retain all accepted source metadata and existing-source behavior | Pass | Seven PR133 and three PR135 source records match accepted heads; an isolated prior supplemental batch survives existing-source application. |
| Catalog and candidate release identity | Pass | `data/catalog.json` and release index remain at `2026-09-17.2`; the candidate manifest and release files are also `2026-09-17.2`. The added coverage snapshot is `2026-09-17.3` with `corpus_version: 2026-09-17.2`. |
| Candidate release regeneration | Pass locally | The 1,095-record release corpus matches the build output, gzip decompresses to the corpus, all five manifest hashes and byte counts match, and the JSONL has 1,095 rows. The source archive has 245 entries with no recovery, `node_modules` or `.git` directory entries. |
| Snapshot provenance and prior immutable artifacts | Pass locally | Snapshot `2026-09-17.3` has 12 matching input hashes and is the only added snapshot; all 19 prior snapshot entries are byte-identical. Fifty-one prior release files, including every 2026-09-17.1 file and older release files, are byte-identical. |
| Guards and existing corpus boundaries | Pass | The diff only extracts the supplemental builder and adds the new-source field; strict newer-state and nested-date tests remain passing. Nonprofit IDs and existing review-status values remain preserved. |
| Issue #119 and #125 criteria | Pass within accepted component scope | The component receipts and catalog retain bounded US role, framework, period, population, source, rights, professional-review and empirical limits. This re-review does not close either issue or claim professional or production acceptance. |

## Verification performed

- `npm run check` at exact head: pass, 101/101 tests, typecheck, validation, UI and design lint, build, MCP transports, release parity, source archive parity, snapshot immutability, nonprofit preservation and package regressions. The build reports 1,095 records, 27 downloads and a 245-file source archive.
- Direct package tests: 13/13 passed for `tests/aa-int119125-integration.test.mjs`, `tests/management-accounting.test.mjs` and `tests/us-operating-transactions.test.mjs`.
- Clean source-absent base replay: first application added the expected package files, the FAR source matched accepted PR135 byte-for-byte, and the second application changed zero of the ten integration files.
- Accepted source comparison: seven PR133 source records and three PR135 source records matched their accepted component heads with no differing fields.
- Snapshot audit: `2026-09-17.3` input hashes match current bytes; no existing snapshot changed.
- Release audit: candidate `2026-09-17.2` manifest hashes match current artifacts; corpus, gzip and JSONL agree; all prior release files outside the candidate `.2` directory are unchanged.
- `git diff --check`: pass.
- No merge, deployment, issue closure, snapshot rewrite, release publication or author-branch mutation was performed.

## Next action

Strengthen the integration regression to exercise a source-absent base and assert the complete accepted source payload plus all-file replay stability. After that test-only correction, the runtime integration and artifact evidence support re-review for integration acceptance. Issue completion and release publication remain separate decisions.
