# Utilities current integration

Edition2026-09-19.12411 integrates source proposal53166a6 after the accepted trade candidate. The source package retains its original proposal status and baseline as provenance; the canonical guide, workflow, control, example and three sources are applied by this integration. The original research disposition describes the source branch, and this document records its current application.

The separate utilities inventory reconstructs the source proposal's immutable baselineeecdd003, lists associated records and linked questions, and records reuse and scope dispositions. Five named questions and five partial assessments address the selected FERC electric-utility role. Other utilities and current consolidated financial-reporting conclusions remain explicit gaps.

The helper's `--current` mode preflights input edition12410 or output12411, stages all writes, preserves JSON serialization and updates the coverage edition metadata. Original source-only application remains available with numeric version comparison, including rejection of numerically older editions. No prior record, source metadata, right or question is replaced. Standard coverage generation updates the related utilities profile's discovery links from the new named questions.

Run `node scripts/integrate-utilities.mjs --current` and the standard coverage, mapping, snapshot and release generators. Dry-run mode changes no files. Source package and current corpus versions remain distinct. No posting, deployment, professional review or operational evidence is implied by the synthetic material.

Applied-corpus validation caught a source-proposal defect that schema-only checks did not exercise: the synthetic example lacked the required scenario array and exact Synthetic label. The integration adds three named scenarios using the existing fictional evidence, amounts and counterexamples, and records the correction without changing the preserved source commit.

The five research question rows also required explicit depth dimensions. Each content dimension is partial, with a stated basis in the selected answer, workflow, controls and fictional worked material; empirical support remains not assessed. Applied corpus validation and retrieval are required before this integration is accepted.
